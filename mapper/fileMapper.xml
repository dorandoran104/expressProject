<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="fileMapper">
  <select id="getFile">
    SELECT
      idx
      ,REPLACE(path,'public/','/') as path
      ,save_name
      ,real_name
      ,DATE_FORMAT(regist_date,'%Y_%m-%d') as regist_date
    FROM file
    <where>
      <if test="file_arr != null and file_arr != ''">
        idx IN (${file_arr})
      </if>
      <if test="file_idx != null and file_idx != ''">
        idx = #{file_idx}
      </if>
    </where>
  </select>
</mapper>